import { ExtensionStatus } from './ExtensionStatusEnum';

export default class Globals {
    public static ExtensionStatus = ExtensionStatus;

    public static RequiredExtensionVersion: number = 1.0;
}